name: tailor_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # UI & Styling
  flutter_svg: ^2.0.7
  google_fonts: ^6.1.0
  cupertino_icons: ^1.0.8

  # Image & Media
  image_picker: ^1.0.4
  mime: ^1.0.4

  # Location & Maps - BOTH Google Maps AND OpenStreetMap
  location: ^6.0.2                    # ✓ KEPT - Your original
  google_maps_flutter: ^2.5.0         # ✓ KEPT - Your original
  geocoding: ^2.1.1                   # ✓ KEPT - Your original
  geolocator: ^10.1.0                 # ✓ KEPT - Your original
  flutter_map: ^6.1.0                 # ✓ KEPT & UPDATED - For OpenStreetMap
  latlong2: ^0.9.1                    # ✓ KEPT - Your original
  permission_handler: ^11.0.1         # ✓ KEPT - Your original

  # Network
  http: ^1.2.2
  http_parser: ^4.0.2

  # State Management
  provider: ^6.1.2

  # Storage
  flutter_secure_storage: ^9.0.0

  # Utilities
  path: ^1.8.3
  intl: ^0.18.0
  url_launcher: ^6.2.0

  # Payment
  razorpay_flutter: ^1.3.7

dev_dependencies:
  flutter_test:
    sdk: flutter
#  flutter_native_splash: ^2.3.10
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/